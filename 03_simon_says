def echo(x)
  "#{x}"
end


def shout(x)
  "#{x.upcase}"
end


def repeat(x, y = 2)
  ([x] * y).join ' '
end


def start_of_word (x, y)
  x[0..(y - 1)]
end


def first_word (x)
  x.split[0]
end


def titleize (x)
  x.split.map(&:capitalize).join(' ')
end
